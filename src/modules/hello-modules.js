/**
 * Возвращает приветственное сообщение с именем
 * @param {string} name -
 * @returns {string} 
 */
function getHelloMessage(name) {
    return `Hello, ${name}`;
}

module.exports = {
    getHelloMessage
};

